<?php
	/**
	 * 
	 * @param unknown $x
	 * @param string $lag_max
	 * @param string $type
	 * @param string $na_rm
	 * @param string $demean
	 */
	function acf($x, $lag_max = NULL, $type = "correlation", 
			$na_rm = "na.fail", $demean = true) {
		
		if($type == "partial") {	
			
		}
		
		
		$n = sizeof($x);
		foreach ($x as $val) {
			
		}
		
	}
	
	

?>
